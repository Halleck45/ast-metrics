name: Go

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: [ '1.21.x' ]

    steps:
      - uses: actions/checkout@v4

      # Test suite for Go parser
      - name: Setup Go ${{ matrix.go-version }}
        uses: actions/setup-go@v4
        with:
          go-version: ${{ matrix.go-version }}
          cache-dependency-path: subdir/go.sum

      - name: Install dependencies
        run: |
          sudo apt install -y make cmake
          make build-deps-libgit

      - name: Display Go version
        run: go version

      - name: Install dependencies
        run: go get .

      - name: Test with the Go CLI
        run: |
          ls ./build/libgit/libgit2-1.5.0/build
          env
          echo $PKG_CONFIG_PATH
          go test
        env:
          LD_LIBRARY_PATH: ./build/libgit/libgit2-1.5.0/build
          PKG_CONFIG_PATH: ./build/libgit/libgit2-1.5.0/build